# Supabase 連線設定
# 取得位置：Supabase Dashboard → Project Settings → API
#
# 本機開發：複製這個檔案為 .env.local，填入下面兩個值（.env.local 不會進 git）
# 正式部署：把同樣兩個值設成 GitHub Repo 的 Secrets（見 README / deploy.yml）

# Project URL（形如 https://xxxxxxxx.supabase.co）
VITE_SUPABASE_URL=

# anon public key（Project API keys 裡的 anon / public 那把）
VITE_SUPABASE_ANON_KEY=
